# RYB Wound Analyzer - Frontend

React + TypeScript + Vite + Tailwind CSS frontend for the RYB Wound Analyzer.

## Run Locally

1. `npm install`
2. Start the FastAPI backend (see `backend/README.md`)
3. `npm run dev`

The Vite dev server proxies `/api` requests to `http://localhost:8000`.
